/**
 * Google Health setup placeholders.
 *
 * Replace both values after following GOOGLE_HEALTH_SETUP.md. The OAuth client
 * ID is public by design; the client secret belongs only in the broker's secret
 * store and must never be added here.
 */
export const GOOGLE_HEALTH_CLIENT_ID = 'REPLACE_WITH_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com'
export const GOOGLE_HEALTH_BROKER_URL = 'https://REPLACE_WITH_GOOGLE_HEALTH_BROKER_URL'
